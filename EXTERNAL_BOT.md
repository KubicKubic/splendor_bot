# 外部公开 Bot 对照（纯 JAX）

本对照固定公开项目 [edwadli/splendor-ai](https://github.com/edwadli/splendor-ai) 的
`ValueBuyBot` 为外部锚点，源提交为
`580687e8f38377f00dff50cc97414308ec4e9c6e`，许可证为 Apache-2.0。原仓库的
`ValueBuyBot` 会按卡牌分数/预计购买节奏选择主卡和提供折扣的前置卡，并处理抢订、缺币、
银行稀缺度和超出 10 枚后的弃币。它是公开、可审计的规则型 bot，不应被理解为已知的人类高手水平。

## JAX 重写与一致性

正式对战使用 [`splendor/valuebot_jax.py`](splendor/valuebot_jax.py)，不是在 Python 循环中
调用原 bot。可变长度字典和卡牌路径被改写为固定的 15 个可见目标、最多 240 条候选路径；
估值、稳定排序、抢订阈值、取币及弃币顺序均保留。模型、JAX bot、每局弃币 carry、
HullQin 状态转移和终局判定全部位于同一个 `jax.jit`/`vmap` 循环内。

原 Python 代码只用于差分测试：在 2,048 个随机轨迹状态中抽取超过 500 个正常阶段局面，
逐张比较卡牌估值，并逐项比较主/次目标、动作和弃币；另连续比较 180 个实际对局动作。
三项 JAX bot 测试全部通过。正式 6,144 局中检测到的非法 bot 动作为 **0**，遇到非法动作时
评估器会直接拒绝整次结果，不会换用备用启发式。

适配边界如下：

- 对局由本站对齐 HullQin 的 JAX 引擎执行，外部项目只提供决策逻辑。
- bot 与模型都看不到未来牌序或对手预定牌身份；bot 仅接收规则允许的公开信息。
- 原 bot 未表达可选黄金付款和多贵族选择；适配器使用最少黄金付款、首个可选贵族。
- 正式对照限定双人局，因此原 bot 遍历所有对手的逻辑与 JAX 版完全等价。

## 固定对照结果

每个检查点进行 2,048 局：1,024 个固定初始牌局各进行两次并交换座位；三个检查点复用
相同牌局和策略随机数。模型统一以 BF16、温度 1 按策略分布采样，外部 bot 为确定性策略。
固定 seed 为 `20261012`，每局上限 20,000 个微决策。

| 模型 | 累计训练决策 | 胜 / 负（自然完赛） | 未完成 | 完赛局得分率（95% CI） | 外部锚点 Elo |
|---|---:|---:|---:|---:|---:|
| initial_131m | 0.131B | 1827 / 105 | 116 | 94.57% [93.44%, 95.56%] | 1495 |
| before_long_1.180b | 1.180B | 1886 / 30 | 132 | 98.43% [97.84%, 98.97%] | 1717 |
| final_6.423b | 6.423B | 1895 / 4 | 149 | **99.79% [99.57%, 99.95%]** | 2050 |

继续到 8.520B 决策后，同种子新增对照为 1,887 胜、8 负、153 未完成；自然完赛得分率
99.58% `[99.26%, 99.84%]`，平均结束长度由 60.15 增至 74.39 玩家回合。直接对战仍显示
新模型比 6.423B 模型高 22.9 Elo，因此这是对固定 bot 的效率退化，而不是整体实力下降。

外部 bot 固定为 Elo 1000；Elo 采用每组 0.5 个虚拟胜、0.5 个虚拟负以避免全胜时无穷大。
它只是这个直接对局协议下的换算，既不是人类 Elo，也不应跨外部 bot 比较。接近饱和时 Elo
对少数败局很敏感，因此胜负数和区间是主要结论。

未完成局没有计入点估计。若将其全部判模型负/胜，三个模型在全部 2,048 局上的得分率边界
分别为 `[89.21%, 94.87%]`、`[92.09%, 98.54%]`、`[92.53%, 99.80%]`。
从 4,000 提高至 20,000 决策只多完成一局；未完成局的回合数中位数接近 20,000，属于允许
pass 后两个策略形成的循环，而非正常对局稍慢。它们被完整保留以显示这一策略缺陷。

单张 A100 上编译耗时 9.29 秒；编译后每个 2,048 局批次约 14.5 秒。由于仍有循环局，
每批都执行满 20,000 步，即约 282 万个批内局面步/秒。机器可读协议、逐局结果和汇总位于
[`runs/external_jax_final`](runs/external_jax_final)。

## 复现

```bash
cd /mnt/pfs/guoyuchong/guoyuchong/spld
XLA_PYTHON_CLIENT_PREALLOCATE=false ../generals_bot/.conda_envs/generals_bot/bin/python \
  -m splendor.evaluate_external \
  --checkpoints runs/a100_2p/policy_000500.npz \
                runs/a100_2p_fast/policy_001000.npz \
                runs/a100_2p_long/policy_006000.npz \
  --names initial_131m before_long_1.180b final_6.423b \
  --games 2048 --seed 20261012 --max-decisions 20000 \
  --out runs/external_jax_final
```

上游源码保持未修改，位于 [`vendor/edwadli_splendor`](vendor/edwadli_splendor)，许可证原文为
[`vendor/edwadli_splendor/LICENSE`](vendor/edwadli_splendor/LICENSE)。评估协议记录上游提交、
模型与 JAX 实现 SHA-256、JAX 版本和 GPU 型号，目录内容变化时要求使用新的输出目录。
